#include "main.h"

/**
 * _abs - function
 * @n: number we look for the absolute value
 * Return: Always 0.
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
	return (0);
}

