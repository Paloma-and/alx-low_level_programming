/**
 * print_sign - function
 *
 * @n: number being tested
 *
 * Return: 1 if @n is greater than 0, 0 if zero
 * -1 if less than zero
 */
int print_sign(int n)
{
	if (n > 0)
	{
		return (1);
	}
	else if (n < 0)
	{
		return (-1);
	}
	else
	{
	return (0);
	}
}
