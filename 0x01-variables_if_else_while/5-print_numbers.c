#include<stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int x;

	char space = '\n';

	for (x = 0; x < 10 ; x++)
	{
	printf("%d", x);
	}
	putchar(space);
		return (0);
}
