#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */

void more_numbers(void)
{
	int i, j;
	char space = '\n';

	for (i = 0; i <= 10; i++)
	{
	for (j = 0; j <= 14; j++)
	{
	_putchar(j);
	}
	_putchar(space);
	}
	_putchar(space);
		return;
}
